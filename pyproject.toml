[project]
name = "ffdl"
version = "0.1.0"
description = "A tool to extract and download files from FitGirl Repacks via fuckingfast.co using Surge."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "lxml>=6.0.2",
    "requests>=2.31.0",
]
